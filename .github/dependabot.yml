version: 2
updates:
  # GitHub Actions are SHA-pinned; dependabot bumps the pin and the # version
  # comment together.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"

  # Keeps the cwtools server submodule pin fresh.
  - package-ecosystem: gitsubmodule
    directory: /
    schedule:
      interval: weekly

  # Extension client (root) and the packaged manifest (release/).
  - package-ecosystem: npm
    directories:
      - /
      - /release
    schedule:
      interval: weekly
    groups:
      npm:
        patterns:
          - "*"
